List the document keys at and below a key that carry none of the named
metadata: exactly what a `get_documents` survey by that metadata cannot show,
since a survey can only report documents that have it, and which
`without_meta` only counts. A document counts as covered when it has any one of
the names given, so ask for one name at a time unless you mean 'none of these'.
