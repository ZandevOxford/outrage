rage.store
==========

``store`` re-exports :class:`rage.keys.Key` for callers that hold a store and
nothing else. It is documented under :mod:`rage.keys`, where it is defined,
and excluded here so that it is described in one place only.

.. automodule:: rage.store
   :exclude-members: Key
